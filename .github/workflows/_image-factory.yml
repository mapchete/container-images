name: _image-factory
on:
  workflow_call:
    inputs:
      image-name:
        required: true
        type: string
      image-tag:
        type: string
        default: ''
      mapchete-ref:
        type: string
        default: ''
      mapchete-eo-ref:
        type: string
        default: ''
      mapchete-hub-ref:
        type: string
        default: ''
      mapchete-hub-cli-ref:
        type: string
        default: ''

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      run-build: ${{ steps.check.outputs.run }}
      mapchete-ref: ${{ steps.resolve.outputs.mapchete }}
      mapchete-eo-ref: ${{ steps.resolve.outputs.eo }}
      mapchete-hub-ref: ${{ steps.resolve.outputs.hub }}
      mapchete-hub-cli-ref: ${{ steps.resolve.outputs.cli }}
      calculated-tag: ${{ steps.version.outputs.tag }}
      mode: ${{ steps.version.outputs.mode }}
      use_branch: ${{ steps.version.outputs.use_branch }}
      target_latest: ${{ steps.version.outputs.target_latest }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: check
        run: |
          if [[ "${{ github.event_name }}" == "repository_dispatch" ]]; then
            S="${{ github.event.client_payload.source_repo }}"
            T="${{ inputs.image-name }}"
            if [[ "$T" == "mapchete" && "$S" =~ ^mapchete(-eo|-hub)?$ ]]; then echo "run=true" >> $GITHUB_OUTPUT
            elif [[ "$T" =~ ^mhub-cli-(light|full)$ && "$S" == "mapchete-hub-cli" ]]; then echo "run=true" >> $GITHUB_OUTPUT
            else echo "run=false" >> $GITHUB_OUTPUT; fi
          else echo "run=true" >> $GITHUB_OUTPUT; fi

      - id: version
        name: Detect Mode and Target Branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EVENT="${{ github.event_name }}"
          P_TYPE="${{ github.event.client_payload.type }}"
          P_TAG="${{ github.event.client_payload.image_tag }}"
          PREFIX="$(date +'%Y.%-m')."
          
          # Default state
          USE_BRANCH="${{ github.ref_name }}"
          MODE="rolling"
          TARGET_LATEST="false"

          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            TAG_NAME="${{ github.ref_name }}"
            MODE="release"
            TARGET_LATEST="true"
          elif [[ "$EVENT" == "repository_dispatch" && "$P_TAG" =~ ^(latest|dev)$ ]]; then
            TAG_NAME="$P_TAG"
            [[ "$P_TAG" == "latest" ]] && TARGET_LATEST="true"
          elif [[ "$P_TYPE" == "version" ]] || [[ "$EVENT" == "workflow_dispatch" && -z "${{ inputs.image-tag }}" ]]; then
            # RC BRANCH DETECTION
            EXIST_RC=$(git branch -r | grep "origin/rc/${{ inputs.image-name }}-$PREFIX" | head -n 1 | sed 's/.*origin\///' || echo "")
            if [[ -n "$EXIST_RC" ]]; then
              TAG_NAME=$(echo "$EXIST_RC" | sed 's/.*-//')
              USE_BRANCH="$EXIST_RC"
              MODE="rc"
            else
              TAGS=$(gh api /orgs/mapchete/packages/container/${{ inputs.image-name }}/versions --jq '.[].metadata.container.tags[]' 2>/dev/null || echo "")
              LATEST_CTR=$(echo "$TAGS" | grep "^$PREFIX" | sed "s/$PREFIX//" | sort -nr | head -n1)
              [[ -z "$LATEST_CTR" ]] && TAG_NAME="${PREFIX}0" || TAG_NAME="${PREFIX}$((LATEST_CTR + 1))"
              MODE="rc"
            fi
            TARGET_LATEST="true"
          else
            TAG_NAME="dev"
          fi

          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "mode=$MODE" >> $GITHUB_OUTPUT
          echo "use_branch=$USE_BRANCH" >> $GITHUB_OUTPUT
          echo "target_latest=$TARGET_LATEST" >> $GITHUB_OUTPUT

      - name: Switch to PR Branch (if exists)
        if: steps.version.outputs.mode == 'rc' && steps.version.outputs.use_branch != github.ref_name
        run: git checkout ${{ steps.version.outputs.use_branch }}

      - id: resolve
        run: |
          # This now reads the versions.yml from the RC branch if we switched
          touch versions.yml 
          M_DEF=$(grep '^MAPCHETE_REF:' versions.yml | cut -d' ' -f2 || echo "main")
          EO_DEF=$(grep '^MAPCHETE_EO_REF:' versions.yml | cut -d' ' -f2 || echo "main")
          HUB_DEF=$(grep '^MAPCHETE_HUB_REF:' versions.yml | cut -d' ' -f2 || echo "main")
          CLI_DEF=$(grep '^MAPCHETE_HUB_CLI_REF:' versions.yml | cut -d' ' -f2 || echo "main")

          D_REPO="${{ github.event.client_payload.source_repo }}"
          D_REF="${{ github.event.client_payload.image_tag || github.event.client_payload.sha }}"

          # Logic: Input > Dispatch (if repo matches) > versions.yml (current branch)
          echo "mapchete=${{ inputs.mapchete-ref || (github.event.client_payload.source_repo == 'mapchete' && (github.event.client_payload.image_tag || github.event.client_payload.sha)) || '$M_DEF' }}" >> $GITHUB_OUTPUT
          echo "eo=${{ inputs.mapchete-eo-ref || (github.event.client_payload.source_repo == 'mapchete-eo' && (github.event.client_payload.image_tag || github.event.client_payload.sha)) || '$EO_DEF' }}" >> $GITHUB_OUTPUT
          echo "hub=${{ inputs.mapchete-hub-ref || (github.event.client_payload.source_repo == 'mapchete-hub' && (github.event.client_payload.image_tag || github.event.client_payload.sha)) || '$HUB_DEF' }}" >> $GITHUB_OUTPUT
          echo "cli=${{ inputs.mapchete-hub-cli-ref || (github.event.client_payload.source_repo == 'mapchete-hub-cli' && (github.event.client_payload.image_tag || github.event.client_payload.sha)) || '$CLI_DEF' }}" >> $GITHUB_OUTPUT

      - name: Generate Summary
        if: always()
        run: |
          if [[ "${{ steps.check.outputs.run }}" == "true" ]]; then
            DECISION="## ⏩ BUILD PROCEEDING"
          else
            DECISION="## 🛑 BUILD SKIPPED"
          fi

          MODE="${{ steps.version.outputs.mode }}"
          TAG="${{ steps.version.outputs.tag }}"
          LATEST="${{ steps.version.outputs.target_latest }}"
          
          # Build a visual list of tags for the summary
          # Handle the RC prefixing only for the visual display here
          if [[ "$MODE" == "rc" ]]; then
            FINAL_TAGS="\`rc-$TAG\`"
          else
            FINAL_TAGS="\`$TAG\`"
          fi
          
          # Add latest to the visual list if targeted
          if [[ "$LATEST" == "true" || "${{ github.ref_name }}" == "main" ]]; then
            FINAL_TAGS="$FINAL_TAGS, \`latest\`"
          fi

          echo "$DECISION" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "### 🛠️ Build Preparation Summary" >> $GITHUB_STEP_SUMMARY
          
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY
          echo "| **Target Image** | \`${{ inputs.image-name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Operation Mode** | \`${MODE:-N/A}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Registry Tags** | **$FINAL_TAGS** |" >> $GITHUB_STEP_SUMMARY
          echo "| **Trigger Event** | \`${{ github.event_name }}\` |" >> $GITHUB_STEP_SUMMARY
          
          # 1. Repository Dispatch Details (Conditional)
          if [[ "${{ github.event_name }}" == "repository_dispatch" ]]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "#### 📡 Incoming Dispatch Payload" >> $GITHUB_STEP_SUMMARY
            echo "| Field | Value |" >> $GITHUB_STEP_SUMMARY
            echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY
            echo "| **Source Repo** | \`${{ github.event.client_payload.source_repo }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Event Type** | \`${{ github.event.client_payload.type }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Payload Tag** | \`${{ github.event.client_payload.image_tag || 'N/A' }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Payload SHA** | \`${{ github.event.client_payload.sha || 'N/A' }}\` |" >> $GITHUB_STEP_SUMMARY
          fi

          # 2. Final Resolved Versions for Build Args
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "#### 📦 Resolved Component Versions" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Resolved Reference |" >> $GITHUB_STEP_SUMMARY
          echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY
          echo "| **mapchete** | \`${{ steps.resolve.outputs.mapchete }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **mapchete-eo** | \`${{ steps.resolve.outputs.eo }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **mapchete-hub** | \`${{ steps.resolve.outputs.hub }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **mapchete-hub-cli** | \`${{ steps.resolve.outputs.cli }}\` |" >> $GITHUB_STEP_SUMMARY

  build:
    needs: prepare
    if: needs.prepare.outputs.run-build == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          # This ensures we work on the existing RC branch if one was found
          ref: ${{ needs.prepare.outputs.use_branch }}
          fetch-depth: 0

      - name: Build Local Image
        uses: docker/build-push-action@v5
        with:
          context: ${{ inputs.image-name }}/
          push: false
          load: true
          tags: local-test-image:latest
          build-args: |
            MAPCHETE_REF=${{ needs.prepare.outputs.mapchete-ref }}
            MAPCHETE_EO_REF=${{ needs.prepare.outputs.mapchete-eo-ref }}
            MAPCHETE_HUB_REF=${{ needs.prepare.outputs.mapchete-hub-ref }}
            MAPCHETE_HUB_CLI_REF=${{ needs.prepare.outputs.mapchete-hub-cli-ref }}

      - name: Image Fingerprint Check
        id: image-fingerprint-check
        run: |
          LOCAL_FINGERPRINT=$(docker run --rm --entrypoint /bin/sh local-test-image:latest -c "cat /app/uv_fingerprint" | tr -d '\r\n ')
          echo "fingerprint=$LOCAL_FINGERPRINT" >> $GITHUB_OUTPUT
          if [[ "${{ github.event_name }}" == "repository_dispatch" && "${{ github.event.client_payload.type }}" == "version" ]]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            REMOTE_TAG="latest"
            [[ "${{ github.ref_name }}" != "main" ]] && REMOTE_TAG="dev"
            INSPECT_JSON=$(skopeo inspect docker://ghcr.io/mapchete/${{ inputs.image-name }}:$REMOTE_TAG 2>/dev/null || echo '{"Labels":{}}')
            REMOTE_FINGERPRINT=$(echo "$INSPECT_JSON" | jq -r '.Labels["org.mapchete.image_fingerprint"] // "none"' | tr -d '\r\n ')
            [[ "$LOCAL_FINGERPRINT" == "$REMOTE_FINGERPRINT" ]] && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Integration Tests
        if: inputs.image-name == 'mapchete' && steps.image-fingerprint-check.outputs.changed == 'true'
        run: |
          docker run --rm \
            -e AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }} \
            -e AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }} \
            -e CDSE_S3_ACCESS_KEY=${{ secrets.CDSE_S3_ACCESS_KEY }} \
            -e CDSE_S3_SECRET_KEY=${{ secrets.CDSE_S3_SECRET_KEY }} \
            -e AWS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt \
            local-test-image:latest \
            /bin/bash -c "cd deps/mapchete-eo && uv pip install -e .[test] && uv run pytest"          

      - uses: docker/login-action@v3
        if: steps.image-fingerprint-check.outputs.changed == 'true'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        if: steps.image-fingerprint-check.outputs.changed == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/mapchete/${{ inputs.image-name }}
          flavor: latest=false
          tags: |
            # 1. Manual Tag
            type=raw,value=${{ inputs.image-tag }},enable=${{ inputs.image-tag != '' }}
            
            # 2. Calculated Primary Tag
            type=raw,value=rc-${{ needs.prepare.outputs.calculated-tag }},enable=${{ needs.prepare.outputs.mode == 'rc' }}
            type=raw,value=${{ needs.prepare.outputs.calculated-tag }},enable=${{ needs.prepare.outputs.mode != 'rc' && needs.prepare.outputs.calculated-tag != '' }}
            
            # 3. The 'Latest' Logic
            type=raw,value=latest,enable=${{ needs.prepare.outputs.target_latest == 'true' || github.ref == 'refs/heads/main' }}
            
            # 4. PRs
            type=ref,event=pr

      - name: Tag and Push Image
        if: steps.image-fingerprint-check.outputs.changed == 'true'
        run: |
          # 1. Get the list of tags generated by the 'meta' step
          # metadata-action outputs a newline-separated list
          TAGS="${{ steps.meta.outputs.tags }}"
          
          for TAG in $TAGS; do
            echo "🏷️ Tagging local-test-image:latest as $TAG"
            docker tag local-test-image:latest "$TAG"
            
            echo "🚀 Pushing $TAG"
            docker push "$TAG"
          done

      - name: Update Tracking
        if: steps.image-fingerprint-check.outputs.changed == 'true' && needs.prepare.outputs.mode == 'rc'
        run: |
          SOURCE="${{ github.event.client_payload.source_repo }}"
          SOURCE_VER="${{ github.event.client_payload.image_tag || github.event.client_payload.sha }}"
          FINAL_VER="${{ needs.prepare.outputs.calculated-tag }}"
          
          KEY_NAME=$(echo "${SOURCE//-/_}_REF" | tr '[:lower:]' '[:upper:]')
          
          # 1. Update versions.yml (Additive!)
          sed -i "s|^${KEY_NAME}:.*|${KEY_NAME}: $SOURCE_VER|" versions.yml
          
          # 2. Update CHANGELOG.rst (Smart Append)
          if grep -q "^${FINAL_VER}$" CHANGELOG.rst; then
            # Header exists, append bullet to existing block
            # This looks for the version and appends 3 lines down
            sed -i "/^${FINAL_VER}$/{n;n;a * Aligned **${SOURCE}** to \`${SOURCE_VER}\`." -e '}' CHANGELOG.rst
          else
            # New version block
            LEN=${#FINAL_VER}
            UNDERLINE=$(printf '%0.s-' $(seq 1 $LEN))
            ENTRY="${FINAL_VER}\n${UNDERLINE}\n\n* Aligned **${SOURCE}** to \`${SOURCE_VER}\`.\n"
            sed -i "4i $ENTRY" CHANGELOG.rst
          fi

      - name: Automated Version PR
        if: steps.image-fingerprint-check.outputs.changed == 'true' && needs.prepare.outputs.mode == 'rc'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.MAPCHETE_PAT || secrets.GITHUB_TOKEN }}
          commit-message: "rc: update ${{ github.event.client_payload.source_repo }} to ${{ github.event.client_payload.image_tag || github.event.client_payload.sha }}"
          branch: "rc/${{ inputs.image-name }}-${{ needs.prepare.outputs.calculated-tag }}"
          title: "🧪 RC: ${{ inputs.image-name }} ${{ needs.prepare.outputs.calculated-tag }}"
          body: |
            ### 🧪 Release Candidate Update
            This PR tracks the alignment for version `${{ needs.prepare.outputs.calculated-tag }}`.
            
            **Latest Update:** Aligned `${{ github.event.client_payload.source_repo }}` to `${{ github.event.client_payload.image_tag || github.event.client_payload.sha }}`.
            
            **Test Image:** `ghcr.io/mapchete/${{ inputs.image-name }}:rc-${{ needs.prepare.outputs.calculated-tag }}`
            **Secondary Tag:** `ghcr.io/mapchete/${{ inputs.image-name }}:latest` (Updated)
            
            **Reviewer Note:** Multiple component updates may be bundled into this PR if dispatches arrive before merging.
          base: main
          delete-branch: true
          assignees: "scartography"
          reviewers: "ungarj"

  cleanup-registry:
    needs: [prepare, build]
    if: always() && needs.prepare.outputs.run-build == 'true'
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Cleanup
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TARGETS=$(gh api --paginate "/orgs/mapchete/packages/container/${{ inputs.image-name }}/versions" --jq '.[] | select(.metadata.container.tags | length == 0) | .id')
          for ID in $TARGETS; do gh api --method DELETE "/orgs/mapchete/packages/container/${{ inputs.image-name }}/versions/$ID" || true; done